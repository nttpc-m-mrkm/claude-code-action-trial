# claude-code-action を試す手順

## 概要

PRを出すと、GitHub Actions上でClaudeがコード差分を読み取り、
`docs/design.md`（内部設計書）を自動で更新してPRにコミットする。

**これがやりたかったことに一番近いツール。**

## 前提

- GitHubアカウントがあること
- GitHubにリポジトリを作成できること

## ステップ1: Anthropic APIキーを取得する（無料）

1. https://platform.claude.com/ にアクセス（※ 普段使ってるClaude Teamとは別）
2. **個人のメールアドレス**でアカウントを新規作成
3. https://platform.claude.com/settings/keys でAPIキーを作成
4. `sk-ant-...` で始まるキーをコピーしてメモしておく

**料金について:**
- 新規アカウントには無料クレジットが付与される（$5程度）
- 1回のPRあたり数円〜数十円なので、数十回は試せる
- クレジットカード登録は不要

※ もし無料クレジットの付与がなくなっている場合は、$5〜10程度のクレジット購入が必要

## ステップ2: GitHubにテスト用リポジトリを作成してpush

```bash
cd claude-code-action-trial

git init
git add .
git commit -m "初期コミット: タスク管理APIのコードと設計書"

# パブリックリポジトリとして作成
gh repo create claude-code-action-trial --public --source=. --push
```

**⚠️ セキュリティ注意:**
- 会社のリポジトリではなく、必ず新しいテスト用リポジトリを使うこと

## ステップ3: Claude GitHub Appをインストール

1. https://github.com/apps/claude にアクセス
2. 「Install」をクリック
3. **「Only select repositories」を選択し、`claude-code-action-trial` だけを指定**
4. インストールを完了

## ステップ4: APIキーをGitHubリポジトリのSecretsに登録

1. GitHubで `claude-code-action-trial` リポジトリを開く
2. Settings → Secrets and variables → Actions
3. 「New repository secret」をクリック
4. Name: `ANTHROPIC_API_KEY`
5. Value: ステップ1でコピーした `sk-ant-...` のキー
6. 「Add secret」をクリック

## ステップ5: コードを変更してPRを作成

設計書が自動更新されるか試すために、コードに変更を加える。

### 変更例: 「優先度」フィールドを追加する

```bash
git checkout -b feature/add-priority
```

以下の変更を行う:

**Task.java に `priority` フィールドを追加:**
```java
private Priority priority;  // 追加

public Priority getPriority() { return priority; }  // 追加
public void setPriority(Priority priority) {         // 追加
    this.priority = priority;
    this.updatedAt = LocalDateTime.now();
}
```

**Priority.java を新規作成:**
```java
public enum Priority {
    LOW,
    MEDIUM,
    HIGH
}
```

**TaskService.java の createTask を修正:**
```java
public Task createTask(String title, String description, Priority priority) {
    Task task = new Task();
    task.setTitle(title);
    task.setDescription(description);
    task.setStatus(TaskStatus.TODO);
    task.setPriority(priority);  // 追加
    return taskRepository.save(task);
}
```

**TaskController.java の createTask を修正:**
```java
@PostMapping
public ResponseEntity<Task> createTask(@RequestBody CreateTaskRequest request) {
    Task task = taskService.createTask(
        request.getTitle(),
        request.getDescription(),
        request.getPriority()  // 追加
    );
    return ResponseEntity.status(HttpStatus.CREATED).body(task);
}
```

### コミットしてPRを作成

```bash
git add .
git commit -m "feat: タスクに優先度(priority)を追加"
git push origin feature/add-priority

gh pr create --title "feat: タスクに優先度を追加" --body "タスクにLOW/MEDIUM/HIGHの優先度フィールドを追加"
```

## ステップ6: 自動更新の動作を確認

PRを作成すると、以下が自動で起きる:

1. GitHub Actionsが `.github/workflows/doc-sync.yml` を実行
2. Claudeがコード差分を読み取る
3. `docs/design.md` を変更内容に合わせて更新する
4. 更新をこのPRに自動コミットする

### 確認ポイント

- [ ] PRの「Actions」タブで「内部設計書の自動更新」ワークフローが実行されているか
- [ ] `docs/design.md` にコミットが追加されているか
- [ ] 設計書のデータモデルに `priority` フィールドが追加されているか
- [ ] `Priority` のEnum定義が追記されているか
- [ ] API仕様の `POST /api/tasks` のリクエストに `priority` が追加されているか
- [ ] クラス構成に `Priority` が追加されているか
- [ ] **説明文がちゃんと日本語で書かれているか**（プロンプトで制御可能）

## Swimmとの違い

| 観点 | claude-code-action | Swimm |
|---|---|---|
| ドキュメント形式 | 既存の `.md` をそのまま使える | `.sw.md`（独自形式） |
| 初回ドキュメント作成 | プロンプト次第で自動生成も可能 | 手動で作成が必要 |
| コード変更時の更新 | **コード差分を読んで設計書の文章ごと書き換える** | コードスニペット部分のみ自動更新 |
| 操作 | **PR出すだけ（完全自動）** | PR出すだけ（スニペットのみ） |
| 出力のカスタマイズ | プロンプトで自由に制御 | Swimm側のフォーマットに従う |
| 費用 | API従量課金（1PR数円〜数十円） | Free〜$16/seat/月 |

## 参考記事

- [claude-code-action（公式リポジトリ）](https://github.com/anthropics/claude-code-action)
- [Documentation Syncソリューション（公式）](https://github.com/anthropics/claude-code-action/blob/main/docs/solutions.md)
- [Automate Your Documentation with Claude Code（Medium）](https://medium.com/@fra.bernhardt/automate-your-documentation-with-claude-code-github-actions-a-step-by-step-guide-2be2d315ed45)
- [Claude Code GitHub Actionsを使ってみた（はてな）](https://asahane.hatenablog.com/entry/2025/12/16/231902)
- [Claude Code GitHub Actionsの導入方法（Qiita）](https://qiita.com/aqua_ix/items/ea13034e49afe47b2803)

## うまくいかなかった場合

- GitHub Actionsのログを確認する（Actions タブ → 該当ワークフロー → ログ）
- `ANTHROPIC_API_KEY` がSecretsに正しく登録されているか確認する
- Claude GitHub App がリポジトリにインストールされているか確認する
- 無料クレジットが残っているか https://platform.claude.com/ で確認する
- ワークフローのパスフィルタ（`src/**/*.java`）が正しいか確認する
